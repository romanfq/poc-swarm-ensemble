typer>=0.16     # command-line interface (>=0.16 for click 8.2+)
rich>=13.7      # formatted terminal output
textual>=0.58   # the Swarm Board UI (Ch.10)
pyyaml>=6.0     # every claim, checkpoint, and arbitration file
